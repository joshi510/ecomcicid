# Public values only — Vite inlines VITE_* at build time.
# Point these at the production API and the canonical shop origin.

VITE_API_URL=https://api.example.com/api
VITE_SITE_URL=https://shop.example.com
