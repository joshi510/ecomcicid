# Same-origin /api uses the Vite proxy in local dev so cart cookies stick.
# For a separately hosted API, use the full URL (https://api.example.com/api).
VITE_API_URL=/api

# Canonical site origin for SEO meta, sitemap, and Open Graph URLs
VITE_SITE_URL=http://localhost:5173
